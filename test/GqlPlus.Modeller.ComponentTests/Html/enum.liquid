<table>
  <thead><th>Value</th><th>Aliases</th><th>Enum</th></thead>
  {%- for item in enum.allItems %}
  <tr>
    <td>{{ item.name }}</td>
    <td>{{ item.aliases | join: ", " }}</td>
    <td>{{ item.enum }}</td>
  </tr>
  {%- endfor %}
</table>