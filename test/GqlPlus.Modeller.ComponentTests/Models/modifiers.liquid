{%- for mod in modifiers -%}
  {%- case mod.modifierKind -%}
    {%- when "Opt" %} ?
    {%- when "List" %} []
    {%- else %} [
      {%- if mod.modifierKind == "Param" -%} $ {%- endif -%}
      {{ mod.by }}
      {%- if mod.optional -%} ? {%- endif -%}]
  {%- endcase -%}
{%- endfor -%}