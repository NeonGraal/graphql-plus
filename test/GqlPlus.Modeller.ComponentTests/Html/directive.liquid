<section>
  {%- assign dir = directive[1] %}
  <h2>
    <sub>Directive</sub> {{ dir.name }}
  </h2>
  <p>
    Locations:
    {%- for loc in dir.locations %} {{ loc[0] }}
    {%- unless forloop.last -%},{%- endunless -%}
    {%- endfor %}
  </p>
  <p> Repeatable: {{ dir.repeatable }} </p>
</section>